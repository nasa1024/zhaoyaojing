# AICheck365 阶段一（证据实验室）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 AICheck365 从"单一 AI 概率上传框"重塑为"AI 媒体证据实验室"——四状态证据结果、真实 C2PA 签名验证、Signal Receipt 导出、Forensic Pop 视觉、方法论/隐私页，全部 9 语言。

**Architecture:** 沿用 Astro SSG + 原生 JS 渐进增强，零新增前端运行时依赖。Rust 引擎在 wasm target 接入 `c2pa` crate（`rust_native_crypto`）做真实签名验证，结果经前端 `state.js` 派生为 A/B/C/D 四状态。`main.js` 拆为 `detect/state/render/receipt/history` 五模块。

**Tech Stack:** Rust + wasm-bindgen + wasm-pack；c2pa-rs；Astro 5；原生 ES Module + CSS 自定义属性；Playwright（e2e）；GA4。

设计来源：`docs/superpowers/specs/2026-06-18-aicheck365-phase1-design.md`。

## Global Constraints

- 文件只在浏览器本地处理，**绝不**上传原始文件、原始文件名、完整 EXIF、GPS、完整 Prompt、私密元数据或文件哈希到任何服务器/分析系统。
- 语义色（强制）：绿=可验证签名信号 · 蓝=中性文件信息 · 黄=弱信号/需注意 · 红=签名失效/字段冲突 · 灰紫=未发现/不确定。**禁止**绿=真人 / 红=AI。
- 结果**绝不**只显示一个 AI 概率；状态 C（未发现信号）**绝不**被描述为"真人创作"，必须附带强制免责文案。
- 状态 A/D 只能来自真实 `ValidationState`；签名无法验证时标为状态 B 的"签名未验证"，**不得**伪造 A/D。
- 核心正文（标题/H1/说明/格式/用法/FAQ/方法论/限制/内部链接/面包屑）必须存在于服务端 HTML；JS 失败时仍可见。
- 所有动效必须遵守 `prefers-reduced-motion`；状态不能只靠颜色（徽章带文字 + 形状/图标）。
- 等宽字体用于：文件名、哈希、字段名、JSON、EXIF 路径、C2PA 字段、MP4 Box、时间戳、生成参数。
- 新文案覆盖全部 9 语言：`zh-CN zh-TW en ja ko de fr es pt-BR`，不半翻译。
- 首屏、上传组件周围、文件选择按钮附近、检测进度区、结果结论附近、导出按钮附近**禁止**广告。
- c2pa crate 桌面端（CLI）与 wasm 端使用同一版本号，避免双版本。
- 频繁提交：每个 Task 末尾提交一次。

---

## 文件结构

**Rust 引擎**
- `Cargo.toml` — wasm target 新增 c2pa 依赖
- `src/web_c2pa_verify.rs`（新建）— 真实 C2PA 签名验证 + `Provenance` 结构 + `ValidationState` 映射
- `src/web.rs`（改）— `BrowserReport` 加 `provenance` 字段；`analyze_*_report` 调用验证；新增 `verify_c2pa` wasm 导出
- `src/lib.rs`（改）— 声明 `web_c2pa_verify` 模块

**前端样式**
- `web/public/tokens.css`（新建）— Forensic Pop 设计令牌
- `web/public/style.css`（改）— 消费令牌；语义色类；新组件样式
- `web/src/layouts/Base.astro`（改）— 引入 tokens.css；导航

**前端脚本（main.js 拆分）**
- `web/src/scripts/detect.js`（新建，承接 main.js 编排）
- `web/src/scripts/state.js`（新建）— `deriveEvidenceState`
- `web/src/scripts/render.js`（新建）— 四状态结果渲染
- `web/src/scripts/receipt.js`（新建）— Signal Receipt 导出
- `web/src/scripts/history.js`（新建）— 历史记录
- `web/src/scripts/main.js`（改）— 仅作为入口 import 上述模块

**前端页面/数据**
- `web/src/components/Detector.astro`（改）— 结果容器 + 专家模式开关
- `web/src/pages/index.astro`（改）— 首页区块
- `web/src/components/{FileAnatomy,EvidenceVanish,FiveLayer,PlatformFingerprint,MethodologyBlock}.astro`（新建）
- `web/src/pages/methodology.astro`（新建）
- `web/src/pages/privacy.astro`（改，经 LocalizedContentPage）
- `web/src/data/{samples.json,experiments.json}`（新建）
- `web/public/scripts/i18n.js`（改）— 9 语言新键

**测试**
- `tests/web_c2pa_verify.rs`（新建，native 可测的纯映射）
- `web/tests/*.spec.js`（新增 e2e）

---

# Group A — 引擎：真实 C2PA 签名验证

### Task A1: 接入 c2pa wasm 依赖并验证可编译

**Files:**
- Modify: `Cargo.toml`

**Interfaces:**
- Produces: wasm target 可用 `c2pa::{Reader, ValidationState}`。

- [ ] **Step 1: 在 Cargo.toml 的 wasm target 段加入 c2pa**

修改 `Cargo.toml` 中 `[target.'cfg(target_arch = "wasm32")'.dependencies]` 段，追加：

```toml
[target.'cfg(target_arch = "wasm32")'.dependencies]
console_error_panic_hook = "0.1"
serde-wasm-bindgen = "0.6"
wasm-bindgen = "0.2"
c2pa = { version = "0.82", default-features = false, features = ["rust_native_crypto"] }
```

同时把桌面端依赖里的 `c2pa = { version = "0.77", ... }` 升级到 `version = "0.82"`（保持两端同版本；保留其 `features = ["file_io", "pdf"]`）。

- [ ] **Step 2: 验证 wasm 能编译（冒烟构建）**

Run:
```bash
rustup target add wasm32-unknown-unknown
cargo build -p aicheck --release --target wasm32-unknown-unknown --no-default-features --features "rust_native_crypto" 2>&1 | tail -20
```
Expected: 编译成功（若报缺 Clang，安装 LLVM/Clang 并设置 `CC`/`AR`；若 0.82 API 不符则按编译错误调整版本）。**这是最高风险点——若此步失败，立即停止并向用户报告，不要继续。**

- [ ] **Step 3: 验证桌面端仍能编译并通过测试**

Run: `cargo test 2>&1 | tail -15`
Expected: 全部通过（确认版本升级没破坏 CLI）。

- [ ] **Step 4: Commit**

```bash
git add Cargo.toml Cargo.lock
git commit -m "build(engine): add c2pa rust_native_crypto dependency for wasm signature verification"
```

---

### Task A2: 纯映射逻辑 `ValidationState` → 状态字符串（native 可测）

**Files:**
- Create: `src/web_c2pa_verify.rs`
- Modify: `src/lib.rs`
- Test: `src/web_c2pa_verify.rs`（内联 `#[cfg(test)]`，native 编译）

**Interfaces:**
- Produces:
  - `pub(crate) enum ProvenanceState { Trusted, Valid, Invalid, Unsigned }`
  - `pub(crate) fn provenance_state_str(s: ProvenanceState) -> &'static str`
  - `pub(crate) struct Provenance { state: String, manifest: Option<ProvenanceManifest>, validation_status: Vec<ValidationIssue>, raw_json: Option<String> }`（`#[derive(Serialize, Default)]`）

> 设计：把**纯逻辑**（枚举→字符串、结构定义）放在对两个 target 都编译的模块里，用 native `cargo test` 测试；真正调用 `c2pa::Reader` 的函数在 Task A3 里加 `#[cfg(target_arch = "wasm32")]`。

- [ ] **Step 1: 在 lib.rs 声明模块（两端都编译）**

修改 `src/lib.rs`，在模块声明区加入（注意**不要**加 `#[cfg(target_arch = "wasm32")]`，让 native 也能编译纯逻辑与测试）：

```rust
pub mod web_c2pa_verify;
```

- [ ] **Step 2: 写失败测试**

创建 `src/web_c2pa_verify.rs`，先写测试：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn maps_validation_states_to_stable_strings() {
        assert_eq!(provenance_state_str(ProvenanceState::Trusted), "trusted");
        assert_eq!(provenance_state_str(ProvenanceState::Valid), "valid");
        assert_eq!(provenance_state_str(ProvenanceState::Invalid), "invalid");
        assert_eq!(provenance_state_str(ProvenanceState::Unsigned), "unsigned");
    }

    #[test]
    fn provenance_default_is_unsigned_empty() {
        let p = Provenance::default();
        assert_eq!(p.state, "unsigned");
        assert!(p.manifest.is_none());
        assert!(p.validation_status.is_empty());
    }
}
```

- [ ] **Step 3: 运行测试确认失败**

Run: `cargo test --lib web_c2pa_verify 2>&1 | tail -15`
Expected: 编译失败（`ProvenanceState`/`Provenance`/`provenance_state_str` 未定义）。

- [ ] **Step 4: 写最小实现**

在 `src/web_c2pa_verify.rs` 顶部（测试模块之前）加入：

```rust
#![cfg_attr(not(target_arch = "wasm32"), allow(dead_code))]

use serde::Serialize;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub(crate) enum ProvenanceState {
    Trusted,
    Valid,
    Invalid,
    Unsigned,
}

pub(crate) fn provenance_state_str(state: ProvenanceState) -> &'static str {
    match state {
        ProvenanceState::Trusted => "trusted",
        ProvenanceState::Valid => "valid",
        ProvenanceState::Invalid => "invalid",
        ProvenanceState::Unsigned => "unsigned",
    }
}

#[derive(Serialize, Clone, Default)]
pub(crate) struct ValidationIssue {
    pub code: String,
    pub url: Option<String>,
    pub explanation: Option<String>,
}

#[derive(Serialize, Clone, Default)]
pub(crate) struct ProvenanceManifest {
    pub title: Option<String>,
    pub format: Option<String>,
    pub claim_generator: Option<String>,
    pub digital_source_type: Option<String>,
    pub assertions: Vec<String>,
}

#[derive(Serialize, Clone)]
pub(crate) struct Provenance {
    pub state: String,
    pub manifest: Option<ProvenanceManifest>,
    pub validation_status: Vec<ValidationIssue>,
    pub raw_json: Option<String>,
}

impl Default for Provenance {
    fn default() -> Self {
        Provenance {
            state: provenance_state_str(ProvenanceState::Unsigned).to_string(),
            manifest: None,
            validation_status: Vec::new(),
            raw_json: None,
        }
    }
}
```

- [ ] **Step 5: 运行测试确认通过**

Run: `cargo test --lib web_c2pa_verify 2>&1 | tail -15`
Expected: 2 passed。

- [ ] **Step 6: Commit**

```bash
git add src/lib.rs src/web_c2pa_verify.rs
git commit -m "feat(engine): add provenance state model and ValidationState mapping"
```

---

### Task A3: wasm 端真实验证函数 `verify_provenance` + `verify_c2pa` 导出

**Files:**
- Modify: `src/web_c2pa_verify.rs`
- Modify: `src/web.rs:165-179`（`BrowserReport` 加字段）, `src/web.rs:231-249`（导出）

**Interfaces:**
- Consumes: A2 的 `Provenance`/`ProvenanceState`。
- Produces:
  - `#[cfg(target_arch = "wasm32")] pub(crate) fn verify_provenance(bytes: &[u8], mime: &str) -> Provenance`
  - `BrowserReport.provenance: Provenance` 字段
  - wasm 导出 `verify_c2pa(bytes, mime) -> JsValue`（返回 `Provenance`）

> 该步的实时验证依赖一个**已签名样本** fixture。TDD 用 native 测纯逻辑（A2 已覆盖）；真实 `Reader::from_stream` 调用通过 wasm 集成验证（Step 4），fixture 来源见 Step 3 备注。

- [ ] **Step 1: 在 web_c2pa_verify.rs 加 wasm 验证函数**

在 `src/web_c2pa_verify.rs` 测试模块之前追加：

```rust
#[cfg(target_arch = "wasm32")]
pub(crate) fn verify_provenance(bytes: &[u8], mime: &str) -> Provenance {
    use c2pa::{Reader, ValidationState};
    use std::io::Cursor;

    let reader = match Reader::from_stream(mime, Cursor::new(bytes)) {
        Ok(reader) => reader,
        Err(_) => return Provenance::default(), // 无 manifest / 不支持 => unsigned
    };

    let state = match reader.validation_state() {
        ValidationState::Trusted => ProvenanceState::Trusted,
        ValidationState::Valid => ProvenanceState::Valid,
        ValidationState::Invalid => ProvenanceState::Invalid,
        ValidationState::Unsigned => ProvenanceState::Unsigned,
    };

    let mut validation_status = Vec::new();
    if let Some(statuses) = reader.validation_status() {
        for s in statuses {
            validation_status.push(ValidationIssue {
                code: s.code().to_string(),
                url: s.url().map(|u| u.to_string()),
                explanation: s.explanation().map(|e| e.to_string()),
            });
        }
    }

    let manifest = reader.active_manifest().map(|m| ProvenanceManifest {
        title: m.title().map(|t| t.to_string()),
        format: m.format().map(|f| f.to_string()),
        claim_generator: Some(m.claim_generator().to_string()),
        digital_source_type: m
            .assertions()
            .iter()
            .find_map(|a| a.label().contains("digitalSourceType").then(|| a.label().to_string())),
        assertions: m.assertions().iter().map(|a| a.label().to_string()).collect(),
    });

    Provenance {
        state: provenance_state_str(state).to_string(),
        manifest,
        validation_status,
        raw_json: Some(reader.json()),
    }
}
```

> 注：`Manifest`/`Assertion` 的访问器名称以 c2pa 0.82 实际 API 为准（编译期校正：若 `claim_generator()`/`assertions()`/`label()` 名称不同，按编译错误调整；目标语义不变——取 claim_generator、digitalSourceType、assertion 标签列表）。

- [ ] **Step 2: `BrowserReport` 加 provenance 字段并接线**

修改 `src/web.rs`：

(a) `BrowserReport` 结构（约 165-179 行）加字段：
```rust
    limitations: Vec<String>,
    provenance: crate::web_c2pa_verify::Provenance,
```

(b) `analyze_image_report` 与 `analyze_video_report` 在构造 `BrowserReport` 时加：
```rust
        limitations: media_limitations("image"), // 或 "video"
        provenance: crate::web_c2pa_verify::verify_provenance(bytes, mime_type),
    })
```
（`analyze_video_frame_rgba` 用 `provenance: Default::default()`。）

(c) 新增 wasm 导出（放在 `analyze_media` 之后）：
```rust
#[wasm_bindgen(js_name = verifyC2pa)]
pub fn verify_c2pa(bytes: &[u8], mime: &str) -> Result<JsValue, JsValue> {
    let provenance = crate::web_c2pa_verify::verify_provenance(bytes, mime);
    serde_wasm_bindgen::to_value(&provenance).map_err(js_err)
}
```

- [ ] **Step 3: 准备已签名 fixture（数据依赖，记录而非造假）**

在 `web/tests/fixtures/` 放一个**真实**含有效 C2PA 的图片 `signed-sample.jpg`（来源：C2PA 官方公开测试资产，或用户提供）。若暂无，在 `web/tests/fixtures/README.md` 记录："State A/D 的 e2e 验证待真实签名样本接入。" **不要**用伪造文件冒充。

- [ ] **Step 4: wasm 集成验证（有 fixture 时）**

Run:
```bash
wasm-pack build --target web --out-dir web/public/pkg 2>&1 | tail -10
```
Expected: 构建成功，`web/public/pkg/aicheck.js` 含 `verifyC2pa` 导出（`grep -c verifyC2pa web/public/pkg/aicheck.js` ≥ 1）。

- [ ] **Step 5: 测量 wasm 体积**

Run: `ls -la web/public/pkg/*.wasm`
Expected: 记录体积。若 > 5MB，在计划备注中标记"考虑把 verifyC2pa 拆为按需懒加载"，但本任务先保证功能正确。

- [ ] **Step 6: native 测试仍通过**

Run: `cargo test 2>&1 | tail -10`
Expected: 通过（native 不编译 wasm 函数，纯逻辑测试仍在）。

- [ ] **Step 7: Commit**

```bash
git add src/web.rs src/web_c2pa_verify.rs web/public/pkg web/tests/fixtures
git commit -m "feat(engine): real C2PA signature verification in wasm + provenance in report"
```

---

# Group B — Forensic Pop 视觉令牌

### Task B1: 创建 tokens.css 并接入 Base 布局

**Files:**
- Create: `web/public/tokens.css`
- Modify: `web/src/layouts/Base.astro:349`（在 style.css 前引入）

**Interfaces:**
- Produces: CSS 变量 `--fp-*`（颜色/字体/半径/动效），`[data-theme="light"]` 覆盖。

- [ ] **Step 1: 写 tokens.css**

创建 `web/public/tokens.css`：

```css
/* Forensic Pop design tokens — 语义色见 Global Constraints */
:root {
  /* 基底（深色为默认实验台） */
  --fp-bone: #F4F0E8;
  --fp-white: #FFFFFF;
  --fp-ink: #151515;
  --fp-graphite: #2A2A2A;

  /* 语义强调色 */
  --fp-verified: #C8FF3D;   /* 绿：可验证签名信号 */
  --fp-neutral:  #275DFF;   /* 蓝：中性文件信息 */
  --fp-weak:     #FFD84D;   /* 黄：弱信号/注意 */
  --fp-fail:     #FF5C55;   /* 红：签名失效/冲突 */
  --fp-unknown:  #8B86A8;   /* 灰紫：未发现/不确定 */

  /* 角色变量（深色主题默认） */
  --bg: var(--fp-ink);
  --surface: #1c1c1c;
  --surface-2: #232323;
  --border: rgba(244,240,232,0.14);
  --text: #ECE7DC;
  --muted: #A39E92;

  /* 字体 */
  --font-display: "Space Grotesk", "Inter", "PingFang SC", "Microsoft YaHei", sans-serif;
  --font-body: "Inter", "PingFang SC", "Microsoft YaHei", sans-serif;
  --font-mono: "JetBrains Mono", "SFMono-Regular", ui-monospace, Menlo, Consolas, monospace;

  --radius: 10px;
  --radius-lg: 16px;
  --motion-fast: 160ms;
  --motion: 280ms;
  --ease: cubic-bezier(0.2, 0.7, 0.2, 1);
}

[data-theme="light"] {
  --bg: var(--fp-bone);
  --surface: var(--fp-white);
  --surface-2: #ECE7DC;
  --border: rgba(21,21,21,0.14);
  --text: var(--fp-ink);
  --muted: #5A564E;
}

@media (prefers-reduced-motion: reduce) {
  :root { --motion-fast: 0ms; --motion: 0ms; }
}
```

- [ ] **Step 2: 在 Base.astro 引入（style.css 之前）**

修改 `web/src/layouts/Base.astro`，把第 349 行附近改为：

```astro
    <link rel="stylesheet" href="/tokens.css" />
    <link rel="stylesheet" href="/style.css" />
```

- [ ] **Step 3: 构建并验证**

Run: `cd web && npm install && npm run build 2>&1 | tail -8`
Expected: 构建成功，`dist/tokens.css` 存在。

- [ ] **Step 4: Commit**

```bash
git add web/public/tokens.css web/src/layouts/Base.astro
git commit -m "feat(ui): add Forensic Pop design tokens"
```

---

### Task B2: style.css 接入令牌 + 语义信号色类

**Files:**
- Modify: `web/public/style.css:1-16`（:root 变量改为引用令牌）, 新增信号色类

**Interfaces:**
- Produces: `.sig-verified .sig-neutral .sig-weak .sig-fail .sig-unknown`（每个含背景/边框/文本 + `::before` 形状标记，**不只靠颜色**）。

- [ ] **Step 1: 把 style.css 的 :root 改为引用令牌**

修改 `web/public/style.css` 顶部 `:root{...}`，删除原硬编码深蓝色值，改为映射到令牌（保留旧变量名以兼容现有规则）：

```css
:root {
  --bg: var(--fp-ink);
  --card: var(--surface);
  --card-strong: var(--surface-2);
  --border: rgba(244,240,232,0.14);
  --border-strong: rgba(244,240,232,0.28);
  --text: #ECE7DC;
  --muted: #A39E92;
  --accent: var(--fp-verified);
  --accent-strong: var(--fp-neutral);
  --danger: var(--fp-fail);
  --danger-text: var(--fp-fail);
  --success: var(--fp-verified);
  --shadow: 0 18px 60px rgba(0,0,0,0.35);
  font-family: var(--font-body);
}
```
同时把 `body` 的 `background` 渐变换成基底色（去掉蓝紫渐变）：`background: var(--bg);`，`font-family: var(--font-body);`。

- [ ] **Step 2: 追加语义信号色类（文末）**

在 `web/public/style.css` 末尾追加：

```css
/* 语义信号色 — 颜色 + 形状双编码（无障碍：状态不只靠颜色） */
.sig { display:inline-flex; align-items:center; gap:6px; padding:3px 10px; border-radius:999px;
  font: 600 12px/1 var(--font-mono); border:1px solid transparent; }
.sig::before { content:""; width:8px; height:8px; }
.sig-verified { color:#0b1a00; background:var(--fp-verified); }
.sig-verified::before { background:#0b1a00; border-radius:50%; }            /* 圆 */
.sig-neutral  { color:#fff; background:var(--fp-neutral); }
.sig-neutral::before { background:#fff; }                                    /* 方 */
.sig-weak     { color:#3a2e00; background:var(--fp-weak); }
.sig-weak::before { background:#3a2e00; clip-path:polygon(50% 0,100% 100%,0 100%); } /* 三角 */
.sig-fail     { color:#fff; background:var(--fp-fail); }
.sig-fail::before { background:#fff; clip-path:polygon(50% 0,100% 50%,50% 100%,0 50%);} /* 菱 */
.sig-unknown  { color:#fff; background:var(--fp-unknown); }
.sig-unknown::before { background:#fff; border-radius:2px; opacity:.6; }
```

- [ ] **Step 3: 构建并目视验证（深/浅主题、对比度）**

Run: `cd web && npm run build 2>&1 | tail -5`
然后人工/截图核对：首页在 dark/light 下文本对比度达标，无残留蓝紫渐变。

- [ ] **Step 4: Commit**

```bash
git add web/public/style.css
git commit -m "feat(ui): map styles to Forensic Pop tokens and add semantic signal classes"
```

---

# Group C — 四状态派生逻辑（state.js）

### Task C: `deriveEvidenceState(report)` 纯函数 + e2e 单测

**Files:**
- Create: `web/src/scripts/state.js`
- Test: `web/tests/state.spec.js`

**Interfaces:**
- Consumes: 引擎 `report`（含 `signals[]`, `provenance{state,manifest,...}`, `limitations[]`）。
- Produces:
  - `export const EVIDENCE_STATE = { A:'A', B:'B', C:'C', D:'D' }`
  - `export function deriveEvidenceState(report) -> { state, level, signature, conflicts:[], aiSignals:[], hasProvenanceAi:boolean }`
  - `export function hasFieldConflict(report) -> boolean`

派生规则（与 spec §4.1 一致）：
- `provenance.state==='invalid'` → **D**（签名失效）。
- 否则若 `hasFieldConflict(report)` → **D**（字段冲突）。
- 否则若 `provenance.state ∈ {trusted,valid}` 且 manifest 指向 AI（`digital_source_type` 含 AI 源类型 或 claim_generator 命中工具）→ **A**（`signature: 'trusted'|'valid'`）。
- 否则若存在 AI 相关元数据信号（source ∈ {C2PA,XMP,PNG,EXIF,MP4,FILE} 且 confidence≠none）→ **B**。
- 否则 → **C**。

冲突判定：同时存在"相机 EXIF 信号"（描述含相机/Make/Model）与"AI 工具信号"（有 tool 字段）；或 `provenance.state==='invalid'`。

- [ ] **Step 1: 写失败测试**

创建 `web/tests/state.spec.js`：

```js
import { test, expect } from '@playwright/test';

// 在浏览器上下文里 import ES module 并执行纯函数
async function derive(page, report) {
  return await page.evaluate(async (r) => {
    const mod = await import('/src/scripts/state.js'); // dev server 下可用；构建后用 /scripts/
    return mod.deriveEvidenceState(r);
  }, report);
}

test.describe('deriveEvidenceState', () => {
  test('A: trusted provenance with AI source', async ({ page }) => {
    await page.goto('/');
    const r = await derive(page, {
      signals: [{ source:'C2PA', confidence:'high', tool:'imagen', description:'x' }],
      provenance: { state:'trusted', manifest:{ digital_source_type:'trainedAlgorithmicMedia', claim_generator:'Imagen' } },
      limitations: [],
    });
    expect(r.state).toBe('A');
    expect(r.signature).toBe('trusted');
  });

  test('D: invalid signature', async ({ page }) => {
    await page.goto('/');
    const r = await derive(page, {
      signals: [], provenance: { state:'invalid', manifest:null, validation_status:[{code:'signingCredential.untrusted'}] }, limitations: [],
    });
    expect(r.state).toBe('D');
  });

  test('B: unsigned AI metadata', async ({ page }) => {
    await page.goto('/');
    const r = await derive(page, {
      signals: [{ source:'XMP', confidence:'medium', tool:'midjourney', description:'AISystemUsed' }],
      provenance: { state:'unsigned', manifest:null }, limitations: [],
    });
    expect(r.state).toBe('B');
  });

  test('C: no signals', async ({ page }) => {
    await page.goto('/');
    const r = await derive(page, { signals: [], provenance: { state:'unsigned', manifest:null }, limitations: [] });
    expect(r.state).toBe('C');
  });

  test('D: camera EXIF + AI tool conflict', async ({ page }) => {
    await page.goto('/');
    const r = await derive(page, {
      signals: [
        { source:'EXIF', confidence:'low', description:'EXIF Make = Canon', tool:null },
        { source:'XMP', confidence:'medium', description:'AISystemUsed', tool:'dall-e' },
      ],
      provenance: { state:'unsigned', manifest:null }, limitations: [],
    });
    expect(r.state).toBe('D');
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `cd web && npx playwright test state.spec.js 2>&1 | tail -20`
Expected: FAIL（state.js 不存在 / 函数未定义）。

- [ ] **Step 3: 写实现**

创建 `web/src/scripts/state.js`：

```js
export const EVIDENCE_STATE = { A: 'A', B: 'B', C: 'C', D: 'D' };

const AI_SOURCE_HINTS = ['trainedalgorithmicmedia','compositewithtrainedalgorithmicmedia',
  'compositesynthetic','algorithmicmedia','datadrivenmedia','trainedalgorithmicdata'];
const CAMERA_HINTS = ['make','model','相机','camera','lens','镜头'];

function manifestPointsToAi(m) {
  if (!m) return false;
  const dst = (m.digital_source_type || '').toLowerCase();
  if (AI_SOURCE_HINTS.some((h) => dst.includes(h))) return true;
  return Boolean(m.claim_generator); // 有签名 + 有生成器，结合 AI 信号视为来源声明
}

function aiSignals(report) {
  return (report.signals || []).filter((s) => (s.confidence || 'none') !== 'none');
}

export function hasFieldConflict(report) {
  if (report.provenance && report.provenance.state === 'invalid') return true;
  const signals = report.signals || [];
  const cameraLike = signals.some((s) =>
    CAMERA_HINTS.some((h) => (s.description || '').toLowerCase().includes(h)) && !s.tool);
  const aiTool = signals.some((s) => Boolean(s.tool));
  return cameraLike && aiTool;
}

export function deriveEvidenceState(report) {
  const prov = report.provenance || { state: 'unsigned', manifest: null };
  const ai = aiSignals(report);
  const conflicts = [];
  let state, signature = null, level;

  if (prov.state === 'invalid') {
    state = EVIDENCE_STATE.D;
    conflicts.push('signature-invalid');
    level = 'fail';
  } else if (hasFieldConflict(report)) {
    state = EVIDENCE_STATE.D;
    conflicts.push('field-conflict');
    level = 'fail';
  } else if ((prov.state === 'trusted' || prov.state === 'valid') && manifestPointsToAi(prov.manifest)) {
    state = EVIDENCE_STATE.A;
    signature = prov.state; // 'trusted' | 'valid'
    level = 'verified';
  } else if (ai.length > 0) {
    state = EVIDENCE_STATE.B;
    level = 'weak';
  } else {
    state = EVIDENCE_STATE.C;
    level = 'unknown';
  }

  return {
    state, level, signature, conflicts,
    aiSignals: ai,
    hasProvenanceAi: manifestPointsToAi(prov.manifest),
  };
}
```

> 注：测试在 dev server（`npm run dev`）下用 `/src/scripts/state.js`。Playwright 配置需指向 dev server——见 Task D1 Step 关于 `webServer` 的配置；若当前配置指向构建产物，则测试 import 路径用 `/scripts/state.js` 并先 `npm run build`。实现 import 路径在生产由 detect.js 用相对路径 `./state.js`。

- [ ] **Step 4: 运行确认通过**

Run: `cd web && npx playwright test state.spec.js 2>&1 | tail -20`
Expected: 5 passed。

- [ ] **Step 5: Commit**

```bash
git add web/src/scripts/state.js web/tests/state.spec.js
git commit -m "feat(web): add 4-state evidence derivation logic with tests"
```

---

# Group D — main.js 拆分 + 四状态结果渲染

### Task D1: 拆分 main.js → detect/history 模块（保持现有行为）

**Files:**
- Create: `web/src/scripts/history.js`, `web/src/scripts/detect.js`
- Modify: `web/src/scripts/main.js`（改为入口）

**Interfaces:**
- Produces: `detect.js` 暴露 `export function initDetector()`；`history.js` 暴露 `export { saveHistory, renderHistory, loadHistoryRaw }`。
- Consumes: 现有 `main.js` 全部逻辑。

> 这是**纯重构**，验证靠现有 e2e 回归（行为不变）。

- [ ] **Step 1: 抽出 history.js**

把 `main.js` 中 History 区块（`HISTORY_KEY` 到 `renderHistory` 及其点击绑定）移入 `web/src/scripts/history.js`，导出 `saveHistory`, `renderHistory`, `loadHistoryRaw`，并 import 所需工具（`escapeHtml`, `labelForConfidence`, `timeAgo`）——这些工具暂时也移入一个 `util.js` 或在 history.js 内联。保持函数体不变。

- [ ] **Step 2: 抽出 detect.js**

把 `main.js` 剩余编排（DOM refs、boot、文件选择、analyze、视频抽帧、renderReports/renderSingleReport/buildReportHtml）移入 `web/src/scripts/detect.js`，包裹为 `export function initDetector(){ ... }`。`main.js` 改为：

```js
import { initDetector } from './detect.js';
initDetector();
```

- [ ] **Step 3: 构建 + 跑现有 e2e 回归**

Run: `cd web && npm run build 2>&1 | tail -5 && npx playwright test 2>&1 | tail -20`
Expected: 现有用例全部通过（行为未变）。

- [ ] **Step 4: Commit**

```bash
git add web/src/scripts/
git commit -m "refactor(web): split main.js into detect/history modules"
```

---

### Task D2: render.js — 四状态结果渲染（11 段结构 + 专家模式 + 决策树）

**Files:**
- Create: `web/src/scripts/render.js`
- Modify: `web/src/scripts/detect.js`（结果改用 render.js）, `web/src/components/Detector.astro`
- Test: `web/tests/result-render.spec.js`

**Interfaces:**
- Consumes: `deriveEvidenceState`（C），引擎 report。
- Produces: `export function renderResult(container, report, { expert=false } = {})`，渲染：结论摘要(状态徽章+语义色) → 证据等级 → 发现的信号(Evidence Card) → 未发现的信号 → 传播/编辑线索 → 原始字段(折叠) → 限制 → 下一步决策树 → 相关平台 → 相关文章 → 导出挂载点 `#receipt-mount`。专家模式显示 `provenance.raw_json`/字段路径/validation_status。

- [ ] **Step 1: Detector.astro 换结果容器 + 专家开关**

修改 `web/src/components/Detector.astro` 的结果区（211-222 行）为：

```astro
<section class="card result-card" id="result-card">
  <div class="section-heading">
    <div>
      <h2 data-i18n="results.title">{copy.results}</h2>
      <p class="muted compact" data-i18n="results.subtitle">{copy.resultsSubtitle}</p>
    </div>
    <label class="expert-toggle"><input type="checkbox" id="expert-mode" /> <span data-i18n="results.expert">专家模式</span></label>
  </div>
  <div id="empty-state" class="empty-state" data-i18n="results.empty">{copy.empty}</div>
  <div id="report" class="report hidden"></div>
</section>
```

- [ ] **Step 2: 写失败测试**

创建 `web/tests/result-render.spec.js`，断言四状态渲染要点（用注入的假 report）：

```js
import { test, expect } from '@playwright/test';

async function render(page, report, expert=false) {
  await page.goto('/');
  return await page.evaluate(async ({ r, e }) => {
    const mod = await import('/src/scripts/render.js');
    const el = document.getElementById('report');
    el.classList.remove('hidden');
    mod.renderResult(el, r, { expert: e });
    return el.innerHTML;
  }, { r: report, e: expert });
}

test('C state shows mandatory disclaimer, not "human-made"', async ({ page }) => {
  const html = await render(page, { signals: [], provenance:{state:'unsigned',manifest:null}, limitations:[], file_name:'x.jpg' });
  expect(html).toContain('未发现'); // 状态 C 文案
  expect(html).not.toContain('真人创作'); // 禁止
  expect(html.toLowerCase()).not.toContain('human-made');
});

test('A state shows verified signal class', async ({ page }) => {
  const html = await render(page, {
    signals:[{source:'C2PA',confidence:'high',tool:'imagen',description:'x',details:[]}],
    provenance:{state:'trusted',manifest:{digital_source_type:'trainedAlgorithmicMedia',claim_generator:'Imagen',assertions:[]}},
    limitations:[], file_name:'x.jpg',
  });
  expect(html).toContain('sig-verified');
});

test('expert mode reveals raw JSON', async ({ page }) => {
  const html = await render(page, {
    signals:[], provenance:{state:'unsigned',manifest:null,raw_json:'{"k":1}'}, limitations:[], file_name:'x.jpg',
  }, true);
  expect(html).toContain('raw_json'); // 专家区出现原始字段标识
});
```

- [ ] **Step 3: 运行确认失败**

Run: `cd web && npx playwright test result-render.spec.js 2>&1 | tail -20`
Expected: FAIL（render.js 不存在）。

- [ ] **Step 4: 写实现**

创建 `web/src/scripts/render.js`（核心骨架，文案走 i18n `t()`；此处给出结构与四状态分支，escapeHtml 从 util/inline）：

```js
import { deriveEvidenceState } from './state.js';

const SIG_CLASS = { verified:'sig-verified', neutral:'sig-neutral', weak:'sig-weak', fail:'sig-fail', unknown:'sig-unknown' };
const LEVEL_CLASS = { verified:'sig-verified', weak:'sig-weak', unknown:'sig-unknown', fail:'sig-fail' };

function esc(v){ return String(v).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])); }

// 翻译器由 detect.js 注入（import { t } from i18n.js 后 setTranslator(t)）。
// 未注入时回退到内置文案——但生产路径必须注入，以满足 9 语言约束。
let _t = null;
export function setTranslator(fn) { _t = fn; }
function tt(key, fallback){ try { const v = _t ? _t(key) : null; return (v && v !== key) ? v : fallback; } catch { return fallback; } }

const STATE_COPY = {
  A: { key:'result.state.a', fallback:'发现可验证的来源凭证', level:'verified' },
  B: { key:'result.state.b', fallback:'发现 AI 相关元数据', level:'weak' },
  C: { key:'result.state.c', fallback:'未发现可识别的来源信号', level:'unknown' },
  D: { key:'result.state.d', fallback:'来源声明无法完成验证', level:'fail' },
};
const STATE_DISCLAIMER = {
  C: { key:'result.state.c.note', fallback:'这不能证明文件由人类创作。来源信息可能从未写入，也可能在截图、压缩、转码或重新导出时被删除。' },
  D: { key:'result.state.d.note', fallback:'文件包含来源信息，但签名失效或字段之间存在冲突，不能给出确定性来源结论。' },
};

export function renderResult(container, report, { expert = false } = {}) {
  const d = deriveEvidenceState(report);
  const sc = STATE_COPY[d.state];
  const sections = [];

  // 1. 结论摘要
  sections.push(`<div class="evidence-summary">
    <span class="sig ${LEVEL_CLASS[sc.level]}">${esc(tt(sc.key, sc.fallback))}</span>
    <strong class="case-file-name">${esc(report.file_name || '—')}</strong>
  </div>`);
  if (STATE_DISCLAIMER[d.state]) {
    sections.push(`<p class="evidence-note">${esc(tt(STATE_DISCLAIMER[d.state].key, STATE_DISCLAIMER[d.state].fallback))}</p>`);
  }

  // 3. 发现的信号
  const found = (report.signals || []);
  sections.push(`<h3>${esc(tt('result.found','发现的信号'))}</h3>` + (found.length
    ? `<div class="signal-list">${found.map(renderEvidenceCard).join('')}</div>`
    : `<p class="muted">${esc(tt('result.none','无'))}</p>`));

  // 4. 未发现的信号
  sections.push(`<h3>${esc(tt('result.notfound','未发现的信号'))}</h3>${renderNotFound(report)}`);

  // 6. 原始字段（折叠）/ 专家模式
  if (expert) sections.push(renderExpert(report));

  // 7. 限制
  const lims = (report.limitations || []);
  sections.push(`<h3>${esc(tt('result.limits','限制说明'))}</h3><ul class="limitation-list">${
    lims.map(l => `<li>${esc(l)}</li>`).join('') || `<li>${esc(tt('result.none','无'))}</li>`}</ul>`);

  // 8. 下一步决策树
  sections.push(`<h3>${esc(tt('result.next','下一步建议'))}</h3>${renderNextSteps(d.state)}`);

  // 9/10/11. 相关平台 / 文章 / 导出挂载
  sections.push(`<div class="related-row" id="related-links"></div>`);
  sections.push(`<div id="receipt-mount"></div>`);

  container.innerHTML = sections.join('\n');
}

function renderEvidenceCard(s) {
  const cls = SIG_CLASS[ s.tool ? 'verified' : 'neutral' ];
  const details = (s.details || []).map(x => `<li><span class="mono">${esc(x.key)}</span>: <span class="mono">${esc(x.value)}</span></li>`).join('');
  return `<article class="evidence-card">
    <div class="evidence-card-head"><span class="mono">${esc(s.source||'')}</span>
      <span class="sig ${cls}">${esc((s.confidence||'low').toUpperCase())}</span>
      ${s.tool ? `<span class="tag-inline">${esc(s.tool)}</span>`:''}</div>
    <div>${esc(s.description||'')}</div>
    ${details ? `<ul class="detail-list">${details}</ul>`:''}
  </article>`;
}

function renderNotFound(report) {
  const layers = [
    ['C2PA', tt('layer.c2pa','C2PA 数字签名')],
    ['XMP', 'XMP'], ['EXIF', 'EXIF'], ['PNG', tt('layer.png','PNG 生成参数')], ['MP4', tt('layer.mp4','视频容器/SEI')],
  ];
  const present = new Set((report.signals||[]).map(s => s.source));
  const items = layers.filter(([k]) => !present.has(k))
    .map(([k,label]) => `<li><span class="sig sig-unknown">—</span> ${esc(label)}</li>`).join('');
  return `<ul class="notfound-list">${items || `<li class="muted">${esc(tt('result.allfound','所有层都发现了信号'))}</li>`}</ul>`;
}

function renderNextSteps(state) {
  const C = ['确认是否为原始文件','尝试获取最早发布版本','检查是否为截图','检查是否经过聊天软件压缩','谨慎参考视觉异常，不要单独依赖','查看对应平台的已知标记','对比其他来源文件'];
  const AB = ['查看该字段的含义','查看平台指纹档案','比较其他平台的来源声明','查看相同平台真实样本','导出检测报告'];
  const list = (state === 'C' || state === 'D') ? C : AB;
  return `<ol class="next-steps">${list.map(x => `<li>${esc(tt('next.'+x, x))}</li>`).join('')}</ol>`;
}

function renderExpert(report) {
  const prov = report.provenance || {};
  return `<details class="expert-block" open><summary>${esc(tt('result.expert','专家模式'))}</summary>
    <div class="kv mono">provenance.state: ${esc(prov.state||'unsigned')}</div>
    ${(prov.validation_status||[]).map(v => `<div class="kv mono">${esc(v.code||'')}</div>`).join('')}
    ${prov.raw_json ? `<pre class="mono raw-json">raw_json:\n${esc(prov.raw_json)}</pre>`:''}
  </details>`;
}
```

- [ ] **Step 5: detect.js 改用 renderResult**

修改 `web/src/scripts/detect.js`：顶部 `import { renderResult, setTranslator } from './render.js'`，在 i18n 的 `t` 就绪后调用一次 `setTranslator(t)`（保证结果文案按当前语言本地化）；`renderSingleReport` 改为 `renderResult(reportEl, report, { expert: expertOn })`；读取 `#expert-mode` 复选框状态，change 时重渲染；`langchange` 事件触发时也重渲染。批量结果每条用 `renderResult` 渲染到各自容器。

- [ ] **Step 6: 运行确认通过 + 回归**

Run: `cd web && npm run build 2>&1 | tail -5 && npx playwright test 2>&1 | tail -20`
Expected: result-render 用例通过，旧用例不回归。

- [ ] **Step 7: Commit**

```bash
git add web/src/scripts/render.js web/src/scripts/detect.js web/src/components/Detector.astro web/tests/result-render.spec.js
git commit -m "feat(web): 4-state evidence result rendering with expert mode and next steps"
```

---

# Group E — Signal Receipt 导出

### Task E: receipt.js — JSON/文本/引用/PNG/打印 + 分享卡

**Files:**
- Create: `web/src/scripts/receipt.js`
- Modify: `web/src/scripts/render.js`（在 `#receipt-mount` 挂载导出按钮）, `web/public/style.css`（`@media print`）
- Test: `web/tests/receipt.spec.js`

**Interfaces:**
- Consumes: report + `deriveEvidenceState`。
- Produces:
  - `export function buildReceiptModel(report)` → `{ file, checked, processing:'Local browser only', lines:[{label,value}], conclusion, disclaimer }`
  - `export function receiptToText(model)` / `receiptToJSON(model)` / `receiptToCitation(model)` → string
  - `export function mountReceipt(container, report)`（渲染按钮：复制文本/复制引用/下载 JSON/下载 PNG/打印）
  - `export function receiptToPngBlob(model)` → Promise<Blob>（canvas）

约束：导出**不含**原始文件、文件哈希、GPS、完整 prompt（buildReceiptModel 只取状态、来源平台、签名状态、信号计数等摘要字段）。

- [ ] **Step 1: 写失败测试**

创建 `web/tests/receipt.spec.js`：

```js
import { test, expect } from '@playwright/test';

async function model(page, report) {
  await page.goto('/');
  return await page.evaluate(async (r) => {
    const m = await import('/src/scripts/receipt.js');
    const mdl = m.buildReceiptModel(r);
    return { mdl, text: m.receiptToText(mdl), json: m.receiptToJSON(mdl), cite: m.receiptToCitation(mdl) };
  }, report);
}

test('receipt omits raw private data', async ({ page }) => {
  const r = { file_name:'secret.jpg', signals:[{source:'C2PA',confidence:'high',tool:'imagen',description:'d',details:[{key:'GPS',value:'1.23,4.56'}]}],
    provenance:{state:'trusted',manifest:{claim_generator:'Imagen',digital_source_type:'trainedAlgorithmicMedia'}}, limitations:[] };
  const { text, json } = await model(page, r);
  expect(text).not.toContain('1.23,4.56'); // 不含 GPS
  expect(json).not.toContain('1.23,4.56');
  expect(text).toContain('SIGNAL RECEIPT');
  expect(text).toContain('Local browser only');
});

test('receipt JSON is valid and has conclusion', async ({ page }) => {
  const r = { file_name:'x.jpg', signals:[], provenance:{state:'unsigned',manifest:null}, limitations:[] };
  const { json } = await model(page, r);
  const parsed = JSON.parse(json);
  expect(parsed.processing).toBe('Local browser only');
  expect(parsed.conclusion).toBeTruthy();
});
```

- [ ] **Step 2: 运行确认失败**

Run: `cd web && npx playwright test receipt.spec.js 2>&1 | tail -20`
Expected: FAIL。

- [ ] **Step 3: 写实现**

创建 `web/src/scripts/receipt.js`：

```js
import { deriveEvidenceState } from './state.js';

const CONCLUSION = {
  A: 'The file contains verifiable AI-origin provenance signals.',
  B: 'The file contains AI-related metadata that is not cryptographically signed.',
  C: 'No recognizable origin signals were found. This is not proof of human creation.',
  D: 'A provenance claim exists but could not be verified, or fields conflict.',
};
const DISCLAIMER = 'This is a provenance reading, not a legal authenticity verdict.';

export function buildReceiptModel(report) {
  const d = deriveEvidenceState(report);
  const prov = report.provenance || {};
  const m = prov.manifest || {};
  const lines = [
    { label: 'File', value: report.file_name || '—' },
    { label: 'Processing', value: 'Local browser only' },
    { label: 'Evidence State', value: d.state },
    { label: 'Signature', value: prov.state || 'unsigned' },
    { label: 'Claim Generator', value: m.claim_generator || '—' },
    { label: 'AI Source Type', value: m.digital_source_type || '—' },
    { label: 'Signals', value: String((report.signals || []).length) },
  ];
  return {
    file: report.file_name || '—',
    checked: new Date().toISOString().slice(0, 10),
    processing: 'Local browser only',
    state: d.state,
    lines,
    conclusion: CONCLUSION[d.state],
    disclaimer: DISCLAIMER,
  };
}

export function receiptToText(model) {
  const body = model.lines.map((l) => `${l.label}: ${l.value}`).join('\n');
  return `AICheck365 — SIGNAL RECEIPT\n\n${body}\n\nConclusion:\n${model.conclusion}\n\nDisclaimer:\n${model.disclaimer}\n\naicheck365.com`;
}
export function receiptToJSON(model) {
  return JSON.stringify({ tool:'AICheck365', ...model }, null, 2);
}
export function receiptToCitation(model) {
  return `AICheck365 provenance reading of "${model.file}" (${model.checked}): state ${model.state}. ${model.disclaimer} — aicheck365.com`;
}

export async function receiptToPngBlob(model) {
  const W = 720, pad = 32, lh = 30;
  const canvas = document.createElement('canvas');
  canvas.width = W; canvas.height = pad*2 + lh*(model.lines.length+6);
  const ctx = canvas.getContext('2d');
  ctx.fillStyle = '#151515'; ctx.fillRect(0,0,canvas.width,canvas.height);
  ctx.fillStyle = '#C8FF3D'; ctx.font = '700 22px monospace';
  ctx.fillText('AICheck365 — SIGNAL RECEIPT', pad, pad+20);
  ctx.fillStyle = '#ECE7DC'; ctx.font = '15px monospace';
  let y = pad + 20 + lh;
  for (const l of model.lines) { ctx.fillText(`${l.label}: ${l.value}`, pad, y); y += lh; }
  y += lh*0.5; ctx.fillStyle = '#8B86A8';
  ctx.fillText(model.disclaimer, pad, y);
  return await new Promise((res) => canvas.toBlob(res, 'image/png'));
}

function download(filename, blob) {
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a'); a.href = url; a.download = filename; a.click();
  setTimeout(() => URL.revokeObjectURL(url), 1000);
}

export function mountReceipt(container, report) {
  const model = buildReceiptModel(report);
  const wrap = document.createElement('div');
  wrap.className = 'receipt-actions';
  wrap.innerHTML = `
    <button data-act="text">复制文字摘要</button>
    <button data-act="cite">复制引用格式</button>
    <button data-act="json">下载 JSON</button>
    <button data-act="png">下载 PNG</button>
    <button data-act="print">打印 / PDF</button>`;
  wrap.addEventListener('click', async (e) => {
    const act = e.target?.dataset?.act; if (!act) return;
    if (act === 'text') navigator.clipboard.writeText(receiptToText(model));
    else if (act === 'cite') navigator.clipboard.writeText(receiptToCitation(model));
    else if (act === 'json') download('aicheck365-receipt.json', new Blob([receiptToJSON(model)], {type:'application/json'}));
    else if (act === 'png') download('aicheck365-receipt.png', await receiptToPngBlob(model));
    else if (act === 'print') window.print();
  });
  container.innerHTML = '';
  container.appendChild(wrap);
}
```

- [ ] **Step 4: render.js 挂载 receipt + 打印样式**

在 `render.js` 的 `renderResult` 末尾、设置 `innerHTML` 之后，调用 `import('./receipt.js').then(m => m.mountReceipt(container.querySelector('#receipt-mount'), report))`。
在 `style.css` 末尾加打印样式：

```css
@media print {
  .site-header, .site-nav, .site-footer, .ad-slot, .upload-card, .receipt-actions, .expert-block { display:none !important; }
  body { background:#fff; color:#000; }
  .evidence-summary, .signal-list, .limitation-list { break-inside: avoid; }
}
```

- [ ] **Step 5: 运行确认通过 + 回归**

Run: `cd web && npm run build 2>&1 | tail -5 && npx playwright test receipt.spec.js 2>&1 | tail -15`
Expected: 通过。

- [ ] **Step 6: Commit**

```bash
git add web/src/scripts/receipt.js web/src/scripts/render.js web/public/style.css web/tests/receipt.spec.js
git commit -m "feat(web): Signal Receipt local export (text/citation/json/png/print)"
```

---

# Group F — 首页区块

> 首页组件均为 Astro 组件，文案走服务端 copy 对象 + `data-i18n`（满足核心正文 SSR）。验证以构建成功 + e2e 关键断言为主。

### Task F1: Hero 重写（上传优先 + 新文案 + 首屏无广告）

**Files:**
- Modify: `web/src/pages/index.astro`（hero copy 块 + 结构）, `web/public/scripts/i18n.js`（hero 新键，仅 zh-CN/en 先到位，其余 Task H 补全）

**Interfaces:**
- Produces: hero 主标题/副标题/隐私行/主次按钮文案键：`hero.h1 hero.lead hero.privacy hero.cta.primary hero.cta.secondary`。

- [ ] **Step 1: 改 index.astro 的 zh-CN/en hero 文案**

把 `homeCopy['zh-CN']` 的 `h1`/`lead` 改为：
```
h1: '别问它"像不像 AI"，看看它留下了什么',
lead: '拖入原始图片或视频。AICheck365 会检查 C2PA、EXIF、XMP、PNG 文本、生成参数和视频容器中的来源线索。',
```
新增 `privacy: '文件在你的浏览器中处理，不会离开你的设备。'`, `ctaPrimary: '拖入原始文件'`, `ctaSecondary: '先拆一个真实样本'`。对 `en` 同步英文版（见 spec §20）。

- [ ] **Step 2: 改 hero 结构（上传入口前置、移除首屏指标占位为证据语义）**

把 hero `<section>` 内的 `hero-panel`（0/100% 指标）替换为隐私说明行 + 主次按钮，主按钮聚焦上传区：
```astro
<p class="hero-privacy" data-i18n="hero.privacy">{copy.privacy}</p>
<div class="hero-cta">
  <a href="#upload-zone" class="btn-primary" data-i18n="hero.cta.primary">{copy.ctaPrimary}</a>
  <button id="open-sample" class="btn-secondary" data-i18n="hero.cta.secondary">{copy.ctaSecondary}</button>
</div>
```
确认 hero 与 `<Detector />` 之间、首屏范围内**没有** `.ad-slot`。

- [ ] **Step 3: 构建 + e2e 断言首屏无广告**

新增 `web/tests/home-hero.spec.js`：
```js
import { test, expect } from '@playwright/test';
test('hero has upload-first CTA and no ad above the fold', async ({ page }) => {
  await page.goto('/');
  await expect(page.locator('.hero-cta a[href="#upload-zone"]')).toBeVisible();
  const adAboveFold = await page.evaluate(() => {
    const ad = document.querySelector('.ad-slot'); if (!ad) return false;
    return ad.getBoundingClientRect().top < window.innerHeight;
  });
  expect(adAboveFold).toBe(false);
});
```
Run: `cd web && npm run build 2>&1 | tail -5 && npx playwright test home-hero.spec.js 2>&1 | tail -15`
Expected: 通过。

- [ ] **Step 4: Commit**

```bash
git add web/src/pages/index.astro web/public/scripts/i18n.js web/tests/home-hero.spec.js
git commit -m "feat(home): evidence-lab hero, upload-first CTA, no above-the-fold ads"
```

---

### Task F2: File Anatomy 视觉组件

**Files:**
- Create: `web/src/components/FileAnatomy.astro`
- Modify: `web/src/pages/index.astro`（hero 内/旁引入）, `web/public/style.css`

**Interfaces:**
- Produces: `<FileAnatomy />`，等宽字体逐层展示 `sample.png` 结构（PNG Header / C2PA Manifest / XMP CreatorTool / digitalSourceType / claim_generator / Embedded Parameters），CSS 逐层展开动画（reduced-motion 关闭）。

- [ ] **Step 1: 写组件**

创建 `web/src/components/FileAnatomy.astro`：
```astro
---
const layers = ['sample.png','├── PNG Header','├── C2PA Manifest','├── XMP CreatorTool','├── digitalSourceType','├── claim_generator','└── Embedded Parameters'];
---
<figure class="file-anatomy" aria-label="文件结构解剖示例">
  <pre class="mono anatomy-tree">{layers.map((l,i) => <span class="anatomy-line" style={`--i:${i}`}>{l}</span>)}</pre>
  <figcaption class="muted" data-i18n="anatomy.caption">AICheck365 查看文件内部，而不是凭画面猜测。</figcaption>
</figure>
```

- [ ] **Step 2: 样式 + 引入**

在 `style.css` 加 `.file-anatomy .anatomy-line{opacity:0;transform:translateY(4px);animation:anat .4s var(--ease) forwards;animation-delay:calc(var(--i)*70ms)}@keyframes anat{to{opacity:1;transform:none}}@media (prefers-reduced-motion:reduce){.anatomy-line{animation:none;opacity:1;transform:none}}`。在 `index.astro` 的 hero 内（上传区旁）引入 `<FileAnatomy />`。

- [ ] **Step 3: 构建验证**

Run: `cd web && npm run build 2>&1 | tail -5`
Expected: 成功；`dist` 首页含 `file-anatomy`。

- [ ] **Step 4: Commit**

```bash
git add web/src/components/FileAnatomy.astro web/src/pages/index.astro web/public/style.css
git commit -m "feat(home): file anatomy visual beside upload zone"
```

---

### Task F3: 滚动叙事一 — 证据如何消失

**Files:**
- Create: `web/src/components/EvidenceVanish.astro`
- Modify: `web/src/pages/index.astro`, `web/public/style.css`
- Create: `web/src/scripts/scroll-narrative.js`（IntersectionObserver 逐步高亮）

**Interfaces:**
- Produces: `<EvidenceVanish />` 展示传播链（原始平台文件 → 聊天压缩 → 社交转码 → 截图 → 重新导出），每步标注消失的信息（C2PA 消失/XMP 删除/EXIF 部分保留/创建时间变化/新软件字段/无法验证）；核心文案 `没有找到证据，不代表它不是 AI。证据可能已经在传播过程中消失。`

- [ ] **Step 1: 写组件 + 内容（服务端可见）**

创建 `web/src/components/EvidenceVanish.astro`，用 `<ol>` 列出 5 步，每步 `<li data-step>` 含"此步丢失"列表（纯 HTML，SSR 可见）。底部强调文案 `data-i18n="vanish.key"`。

- [ ] **Step 2: 渐进增强高亮**

创建 `web/src/scripts/scroll-narrative.js`：用 `IntersectionObserver` 给进入视口的 `[data-step]` 加 `.active`；`prefers-reduced-motion` 时直接全部 `.active`。在 `index.astro` `<script>` 内 import。样式在 `style.css` 加非动画的可读默认 + `.active` 强调。

- [ ] **Step 3: 构建 + 无障碍检查**

Run: `cd web && npm run build 2>&1 | tail -5`
Expected: 成功；禁用 JS 时 5 步与"丢失项"仍可读（SSR 文本存在）。

- [ ] **Step 4: Commit**

```bash
git add web/src/components/EvidenceVanish.astro web/src/scripts/scroll-narrative.js web/src/pages/index.astro web/public/style.css
git commit -m "feat(home): scroll narrative — how evidence disappears"
```

---

### Task F4: 滚动叙事二 — 文件五层解剖

**Files:**
- Create: `web/src/components/FiveLayer.astro`
- Modify: `web/src/pages/index.astro`, `web/public/style.css`

**Interfaces:**
- Produces: `<FiveLayer />` 五层（基础信息 / EXIF+XMP / PNG 文本+生成参数 / C2PA+数字签名 / 视频容器+SEI+帧），每层可点击 `<details>` 展开真实字段示例（等宽）。SSR 可见。

- [ ] **Step 1: 写组件**

创建 `web/src/components/FiveLayer.astro`，5 个 `<details class="evidence-layer">`，每个 `<summary>` 标题 + 内含等宽字段示例（如 `digitalSourceType: trainedAlgorithmicMedia`）。

- [ ] **Step 2: 样式 + 引入 index**

`style.css` 加 `.evidence-layer` 样式（左侧编号、语义中性蓝边）。`index.astro` 引入 `<FiveLayer />`。

- [ ] **Step 3: 构建验证**

Run: `cd web && npm run build 2>&1 | tail -5`
Expected: 成功。

- [ ] **Step 4: Commit**

```bash
git add web/src/components/FiveLayer.astro web/src/pages/index.astro web/public/style.css
git commit -m "feat(home): five-layer file anatomy narrative"
```

---

### Task F5: 平台指纹胶囊区

**Files:**
- Create: `web/src/components/PlatformFingerprint.astro`
- Modify: `web/src/pages/index.astro`, `web/public/style.css`

**Interfaces:**
- Produces: `<PlatformFingerprint />` 胶囊列表（OpenAI/Gemini/Midjourney/Adobe Firefly/Stable Diffusion/ComfyUI/Sora/Kling/Runway/Flux），每个链接到现有 `/platforms/<slug>/`；标题说明"支持研究的平台"，不伪造合作。

- [ ] **Step 1: 写组件（映射到现有平台页）**

创建 `web/src/components/PlatformFingerprint.astro`，平台名→现有 slug 映射（如 OpenAI→`dall-e`, Adobe Firefly→`firefly`, Stable Diffusion→`stable-diffusion`），渲染胶囊链接，带 `prefix`（语言前缀，从 `index.astro` 传入或自算）。

- [ ] **Step 2: 样式 + 引入**

`style.css` 加 `.platform-fingerprint .pill` 圆润胶囊样式。`index.astro` 引入。

- [ ] **Step 3: 构建 + 断言链接**

新增 e2e 断言 `/platforms/dall-e/` 等链接存在。
Run: `cd web && npm run build 2>&1 | tail -5 && npx playwright test home-hero.spec.js 2>&1 | tail -8`

- [ ] **Step 4: Commit**

```bash
git add web/src/components/PlatformFingerprint.astro web/src/pages/index.astro web/public/style.css
git commit -m "feat(home): platform fingerprint capsules linking to platform guides"
```

---

### Task F6: 方法论与限制区 + 广告位规则

**Files:**
- Create: `web/src/components/MethodologyBlock.astro`
- Modify: `web/src/pages/index.astro`（引入 + 在方法论之后放置广告）, `web/public/style.css`

**Interfaces:**
- Produces: `<MethodologyBlock />` 两栏（可检查 / 不能保证，spec §7.9），口号"证据比感觉重要。透明比百分比重要。"；广告 `.ad-slot` 只在该区块**之后**出现且预留高度。

- [ ] **Step 1: 写组件**

创建 `web/src/components/MethodologyBlock.astro`，两个 `<ul>`（can-check / cannot-guarantee），引用 spec §7.9 条目；底部链接到 `/methodology/`。

- [ ] **Step 2: 引入并放置广告（带预留高度）**

`index.astro` 在 `<MethodologyBlock />` 之后放 `.ad-slot`（`style.css` 给 `.ad-slot{min-height:280px}` 预留高度防跳动）。确认现有 Detector 内的 `.ad-slot` 移到结果**之后**而非结论旁（核对 Detector.astro 广告位置符合 Global Constraints）。

- [ ] **Step 3: 构建 + e2e 广告位置断言**

扩展 home-hero.spec.js：断言 `.ad-slot` 的 `top` 在方法论区块之后。
Run: `cd web && npm run build 2>&1 | tail -5 && npx playwright test 2>&1 | tail -15`

- [ ] **Step 4: Commit**

```bash
git add web/src/components/MethodologyBlock.astro web/src/pages/index.astro web/public/style.css web/tests/home-hero.spec.js
git commit -m "feat(home): methodology block + compliant ad placement with reserved height"
```

---

### Task F7: 样本体验 + 最新实验（UI + JSON schema + 占位）

**Files:**
- Create: `web/src/data/samples.json`, `web/src/data/experiments.json`
- Create: `web/src/components/SampleExperience.astro`, `web/src/components/LatestExperiments.astro`
- Modify: `web/src/pages/index.astro`, `web/src/scripts/detect.js`（"先拆一个真实样本"按钮加载样本）

**Interfaces:**
- Consumes: `samples.json`/`experiments.json`（数组，可空）。
- Produces:
  - 数据为空时显示"即将上线"占位，**不展示虚构数据**。
  - 非空时渲染样本卡 / 实验卡（字段见 spec §6.2 schema）。
  - `#open-sample` 按钮：有样本则加载第一个样本走检测流程；无样本则提示"样本即将上线"。

- [ ] **Step 1: 建空数据文件 + schema 注释**

创建 `web/src/data/samples.json` = `[]`，`web/src/data/experiments.json` = `[]`。在同目录加 `README.md` 写明字段 schema（spec §6.2）与"必须真实测试"的要求。

- [ ] **Step 2: 写组件（空数组→占位）**

`SampleExperience.astro` / `LatestExperiments.astro`：`import samples from '../data/samples.json'`，`samples.length ? 渲染卡 : <p class="muted" data-i18n="sample.soon">真实样本即将上线</p>`。

- [ ] **Step 3: `#open-sample` 行为**

在 `detect.js` 监听 `#open-sample`：fetch `/src/data/samples.json`（或构建期注入）；为空则 `setStatus(t('sample.soon'))`；非空则用样本 `fileRef` fetch 文件并走 analyze 流程。

- [ ] **Step 4: 构建验证占位可见**

Run: `cd web && npm run build 2>&1 | tail -5`
Expected: 首页出现"即将上线"占位，无虚构数据。

- [ ] **Step 5: Commit**

```bash
git add web/src/data web/src/components/SampleExperience.astro web/src/components/LatestExperiments.astro web/src/pages/index.astro web/src/scripts/detect.js
git commit -m "feat(home): sample experience + latest experiments UI with data schema and honest placeholders"
```

---

# Group G — 方法论页 + 隐私页

### Task G1: /methodology/ 页面

**Files:**
- Create: `web/src/pages/methodology.astro`
- Modify: `web/src/layouts/Base.astro`（导航/footer 加方法论链接）

**Interfaces:**
- Produces: `/methodology/` 服务端渲染：可检查项 / 不能保证项 / 口号 / 与首页方法论一致；唯一 Title/Description/H1、面包屑、JSON-LD（`Article`）。

- [ ] **Step 1: 写页面**

创建 `web/src/pages/methodology.astro`，复用 `Base`，含 `<h1>`、两栏列表（§7.9）、口号、内部链接（→ 工具、隐私、平台）。加 `BreadcrumbList` + `Article` JSON-LD（`dateModified` 真实）。zh-CN/en 文案先到位（其余 Task H）。

- [ ] **Step 2: 导航接入**

`Base.astro` 的 `NAV_LABELS` 各语言加 `methodology` 标签，nav/footer 加 `<a href={`${navPrefix}/methodology/`}>`。

- [ ] **Step 3: 构建 + e2e**

新增 `web/tests/methodology.spec.js`：访问 `/methodology/`，断言 `<h1>` 与"不能保证"区存在、JS 关闭时正文可见（用 `page.goto` + `javaScriptEnabled:false` context）。
Run: `cd web && npm run build 2>&1 | tail -5 && npx playwright test methodology.spec.js 2>&1 | tail -15`

- [ ] **Step 4: Commit**

```bash
git add web/src/pages/methodology.astro web/src/layouts/Base.astro web/tests/methodology.spec.js
git commit -m "feat(content): methodology page with capabilities and limits"
```

---

### Task G2: 隐私页更新（§17）

**Files:**
- Modify: `web/src/components/LocalizedContentPage.astro`（privacy 文案块）

**Interfaces:**
- Produces: 隐私页明确：文件是否上传 / 哪些本地完成 / 是否保存文件/结果 / 是否收集元数据 / 如何删除数据 / 分析统计排除项。

- [ ] **Step 1: 扩充 privacy 文案**

在 `LocalizedContentPage.astro` 各语言 copy 的 privacy 段补充 §17 要点条目（zh-CN/en 先全，其余 Task H）。明确列出"绝不发送：原始文件、原始文件名、完整 EXIF、GPS、完整 Prompt、私密元数据、文件哈希"。

- [ ] **Step 2: 构建验证**

Run: `cd web && npm run build 2>&1 | tail -5`
Expected: `/privacy/` 含新条目。

- [ ] **Step 3: Commit**

```bash
git add web/src/components/LocalizedContentPage.astro
git commit -m "docs(privacy): align privacy page with local-first data policy"
```

---

# Group H — 9 语言文案 + 分析事件 + SEO

### Task H1: 补全 9 语言新文案键

**Files:**
- Modify: `web/public/scripts/i18n.js`（9 语言新增键）, `web/src/pages/index.astro` 等的服务端 copy 对象（ja/ko/de/fr/es/pt-BR/zh-TW）

**Interfaces:**
- Consumes: F1–F7、D2、E、G 引入的所有新 `data-i18n` 键。
- Produces: 全部新键在 `translations` 的 9 个语言下均有值（无半翻译）。

新增键清单（至少）：`hero.h1 hero.lead hero.privacy hero.cta.primary hero.cta.secondary anatomy.caption vanish.* fivelayer.* fingerprint.title methodology.* sample.soon results.expert result.state.a/b/c/d result.state.c.note result.state.d.note result.found result.notfound result.limits result.next layer.* next.*`。

- [ ] **Step 1: 收集所有新键**

Run: `grep -rhoE "data-i18n(-html)?=\"[^\"]+\"" web/src | sed -E 's/.*\"([^\"]+)\".*/\1/' | sort -u > /tmp/keys.txt && cat /tmp/keys.txt`
Expected: 列出全部使用中的 key。

- [ ] **Step 2: 为 9 语言补值**

在 `i18n.js` 的每个语言对象补齐 /tmp/keys.txt 中缺失的键（人工质量翻译，不机翻直出）。zh-TW 可基于 zh-CN 转写。

- [ ] **Step 3: 校验无缺键**

新增 `web/tests/i18n-coverage.spec.js`：导入 i18n.js，断言每个语言对象都包含 /tmp/keys.txt 列出的关键键集（硬编码关键键列表）。
Run: `cd web && npx playwright test i18n-coverage.spec.js 2>&1 | tail -15`
Expected: 通过（无语言缺键）。

- [ ] **Step 4: Commit**

```bash
git add web/public/scripts/i18n.js web/src/pages/index.astro web/src/components web/tests/i18n-coverage.spec.js
git commit -m "i18n: add Phase-1 copy across all 9 languages"
```

---

### Task H2: 分析事件（§18，零隐私）

**Files:**
- Modify: `web/src/scripts/detect.js`, `web/src/scripts/render.js`, `web/src/scripts/receipt.js`

**Interfaces:**
- Produces: `ga(name, params)` 调用，事件名见 Global Constraints/§18；params **只含**非隐私量（如 `evidence_state`、`file_type`、`signal_count`），绝不含文件名/EXIF/GPS/prompt/hash。

- [ ] **Step 1: 埋点**

在对应位置调用现有 `ga()`：`analysis_started`/`analysis_completed`（带 `evidence_state`, `signal_count`）/`analysis_failed`/`expert_mode_enabled`/`report_exported`（带 `format`）/`sample_opened`/`evidence_card_expanded`。

- [ ] **Step 2: 隐私审查（grep 守卫）**

Run: `grep -nE "gtag|ga\(" web/src/scripts/*.js`
人工核对：无 file_name / exif / gps / prompt / hash 进入参数。

- [ ] **Step 3: 构建验证**

Run: `cd web && npm run build 2>&1 | tail -5`

- [ ] **Step 4: Commit**

```bash
git add web/src/scripts
git commit -m "feat(analytics): anonymous Phase-1 events without private payloads"
```

---

### Task H3: SEO — 新页结构化数据 + 结果 noindex

**Files:**
- Modify: `web/src/pages/methodology.astro`（JSON-LD/breadcrumb）, `web/src/components/Detector.astro`（结果区无索引说明）

**Interfaces:**
- Produces: 方法论页 `Article`+`BreadcrumbList`；结果为客户端私有（页面本身 index，但结果数据不进入 HTML/sitemap）。

- [ ] **Step 1: 校验结构化数据与可见内容一致**

确认 methodology 的 JSON-LD 字段（标题/日期）与页面可见内容一致，无虚假评分/评论。

- [ ] **Step 2: sitemap 检查**

Run: `cd web && npm run build 2>&1 | tail -5 && grep -c "methodology" dist/sitemap*.xml`
Expected: methodology 进入 sitemap（≥1）；结果页/用户数据不出现。

- [ ] **Step 3: Commit**

```bash
git add web/src/pages/methodology.astro web/src/components/Detector.astro
git commit -m "feat(seo): structured data for methodology, keep results out of index"
```

---

# Group I — 全量验证

### Task I: 端到端构建 + 回归 + 无障碍/性能抽查

**Files:** 无新增（验证任务）

- [ ] **Step 1: 引擎全量测试**

Run: `cargo test 2>&1 | tail -10`
Expected: 全绿。

- [ ] **Step 2: wasm 构建**

Run: `wasm-pack build --target web --out-dir web/public/pkg 2>&1 | tail -8`
Expected: 成功，`verifyC2pa` 导出存在。

- [ ] **Step 3: 前端构建 + 全 e2e**

Run: `cd web && npm run build 2>&1 | tail -8 && npx playwright test 2>&1 | tail -25`
Expected: 全部用例通过。

- [ ] **Step 4: 无障碍/减少动效/无 JS 抽查**

- 设 `prefers-reduced-motion: reduce`，确认动画停用。
- 关 JS（`javaScriptEnabled:false`）访问 `/`、`/methodology/`、`/privacy/`，确认主题/说明/格式/FAQ/方法论/链接可见。
- 抽查 dark/light 对比度与"状态不只靠颜色"（徽章带形状）。

- [ ] **Step 5: 验收单核对**

对照 spec §14 验收清单逐条确认（样本体验项标注"待真实数据"）。

- [ ] **Step 6: Commit（如有修订）**

```bash
git add -A && git commit -m "test: Phase-1 end-to-end verification fixes"
```

---

## 备注与已知依赖

1. **真实样本/实验数据**：Task F7 与 A3 的 e2e 验证依赖用户提供真实样本文件，未提供前用占位/记录，**不造假**。
2. **c2pa wasm 体积**：Task A3 Step 5 测量后若过大，追加懒加载子任务。
3. **Playwright import 路径**：dev server 用 `/src/scripts/*.js`；若 CI 用构建产物则改 `/scripts/*.js` 并先 build。执行首个前端任务时统一确认 `web/playwright.config.js` 的 `webServer`/`baseURL`。
