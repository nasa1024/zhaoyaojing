{
  "name": "aicheck365-web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/localize-tool-links.mjs && node scripts/seo-qa.mjs",
    "build:astro": "astro build",
    "qa:seo": "node scripts/seo-qa.mjs",
    "preview": "astro preview",
    "test": "playwright test",
    "test:worker": "node --test tests/worker.test.js tests/localize-tool-links.test.js",
    "test:market-report": "node --test tests/market-report.test.js",
    "report:countries": "node scripts/build-country-performance-report.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.0.0",
    "@ffmpeg/core": "0.12.10",
    "@ffmpeg/ffmpeg": "0.12.15",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
