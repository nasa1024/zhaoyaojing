---
import UnifiedToolHubPage from '../../../components/tools/UnifiedToolHubPage.astro';

export function getStaticPaths() {
  return ['zh-TW', 'en', 'ja', 'ko', 'de'].map((lang) => ({ params: { lang } }));
}
---
<UnifiedToolHubPage />
