---
import UnifiedToolPage from '../../../components/tools/UnifiedToolPage.astro';

export function getStaticPaths() {
  return ['zh-TW', 'en', 'ja', 'ko', 'de'].map((lang) => ({ params: { lang } }));
}
---
<UnifiedToolPage />
