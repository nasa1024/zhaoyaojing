---
import UnifiedToolPage from '../../components/tools/UnifiedToolPage.astro';

export function getStaticPaths() {
  return ['exif-xmp-reader', 'png-parameter-extractor', 'mp4-metadata-inspector']
    .map((tool) => ({ params: { tool } }));
}
---
<UnifiedToolPage />
